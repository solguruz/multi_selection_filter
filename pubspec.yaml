name: multi_selection_filter
description: A package that will help you to create multi selection filter dialog in Flutter.
version: 0.0.4
homepage: https://github.com/solguruz/multi_selection_filter

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0